# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# The built UI is committed on purpose. The API serves it from `web/dist`, and the deployed
# image is built without Node, so the build has to be in the repository. Rebuild it with
# `npm run build` after changing anything under `src`.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
