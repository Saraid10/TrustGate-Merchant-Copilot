/**
 * The Scorecard shows 24 requests with perfect results but no way to see them, and its run time
 * (15:32 today) is later than the demo. Hidden until there is a list of the requests behind it.
 * Set to true to bring back the tab and the 3 key.
 */
export const SHOW_SCORECARD = false;
